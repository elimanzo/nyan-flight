export const AccessibilityNotice = () => (
  <div className="sr-only" aria-live="polite">
    Use space or tap to flap the cat. Press escape to pause. Music starts after the first interaction and can be adjusted in settings.
  </div>
)
