# Issue Tracker

GitHub Issues on this repo.

## Fetching an issue

```bash
gh issue view <NUMBER>
```

Issue numbers appear in commit messages as `(T<N>)` and in `Closes #<NUMBER>` footers.
To list open issues: `gh issue list --state open`
